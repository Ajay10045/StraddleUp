POSTGRES_DB=straddleup
POSTGRES_USER=straddleup
POSTGRES_PASSWORD=change-this-before-sharing
APP_SECRET=replace-with-a-long-random-secret
# Set HOST_ACCESS_CODE only in your ignored local .env file. Do not commit it.
